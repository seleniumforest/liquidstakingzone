{
    "name": "web-monorepo",
    "version": "1.0.0",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "ui",
        "backend",
        "packages/*"
    ],
    "scripts": {
        "start": "yarn --cwd backend start && yarn --cwd ui start"
    }
}